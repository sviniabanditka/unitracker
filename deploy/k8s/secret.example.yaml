# Template only — copy to secret.yaml and fill in real values.
# secret.yaml is gitignored. Apply with:
#   kubectl apply -f deploy/k8s/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: tracker-env
  namespace: tracker
type: Opaque
stringData:
  INITIAL_ADMIN_USERNAME: admin
  INITIAL_ADMIN_PASSWORD: change-me-on-first-login
  SESSION_SECRET: replace-with-32+-random-bytes
